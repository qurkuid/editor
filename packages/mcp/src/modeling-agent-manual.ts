export const MODELING_AGENT_MANUAL = [
  '# Pascal Modeling Agent Manual',
  '',
  'Read this manual before every modeling task. It is the shared operational contract for the in-editor modeling agent and agents controlling Pascal through MCP.',
  '',
  '## Start Here',
  '',
  '1. Identify the execution surface: internal structured-plan agent or external MCP agent.',
  '2. Read the current structured scene. Never infer scene truth from viewport pixels.',
  '3. Resolve the exact target from stable node ids, face ids, selection, and hierarchy.',
  '4. Prefer the narrowest semantic operation that expresses the requested change.',
  '5. Apply one logical user gesture as one undoable mutation.',
  '6. Validate the scene, inspect the resulting state, and verify undo for destructive or topology-changing work.',
  '',
  '## Scene Contract',
  '',
  '- X/Z are the ground plane, Y is up, lengths are metres, and rotations are radians.',
  '- Preserve node ids, parent relationships, Body vertex/edge/loop/face ids, face materials, and UV frames unless the requested operation explicitly replaces them.',
  '- Levels belong to Buildings. Walls, zones, slabs, ceilings, roofs, stairs, Bodies, and floor items belong to a Level or their documented structural parent. Doors and windows belong to a Wall.',
  '- For doors and windows, use `t` or `position` from 0 to 1 along the wall unless the operation schema explicitly documents another coordinate.',
  '- Prefer semantic tools and deterministic operations over raw graph patches. Use a raw patch only when no semantic operation represents the change.',
  '- Keep previews ephemeral. Commit only the final result, once.',
  '- Do not silently invent dimensions, geometry, openings, materials, or unsupported topology. Ask for the smallest missing measurement when it changes the result materially.',
  '',
  '## Internal Modeling Agent',
  '',
  '- The editor injects this entire manual, the current scene, selection, relevant schemas, and conversation into every request.',
  '- Return only the structured modeling plan requested by the editor. Do not call tools or edit repository files.',
  '- Use create/update/delete for ordinary nodes. Updates contain changed fields only.',
  '- Use `imprintBodyFace` on an eligible closed Body face with a coplanar line/rectangle/circle, sampled three-point-arc profile, or regular polygon profile. Arc input is a deterministic 2..256-point line tessellation, defaulting to 32 segments to match the circle convention; the editor exposes authoring-time Arc segments as an integer in that same 2..256 range, and the chosen value is snapshotted for preview and commit. Polygon input is a deterministic counter-clockwise 3..256-sided profile, defaulting to 6 sides; its authoring-time side count is snapshotted for preview and commit. Agents submit the sampled profile through this existing operation rather than inventing a curve operation. Include its optional signed `distance` to create a raised boss or recessed pocket atomically without guessing the generated inset face id; a negative distance whose magnitude reaches or passes the nearest blocking plane becomes a persistent through-cut from the original profile prism (exact boundary contact and modest overshoot both cut through), while an insufficient negative distance stays a pocket and a positive distance stays a boss; omit distance for a flat split. Preserve source face and Boolean cut-surface material/UV provenance. Reject holes, curves, non-planar or degenerate profiles, and multi-shell output atomically. The direct Face Imprint interaction is 3D-only; 2D remains render-only because sketching and face extrusion are 3D operations. Use `pushPullBodyFace` directly for exact existing Body face extrusion, `offsetBodyFace` for a signed planar face-profile offset, `transformBody` for Body translation, arbitrary-axis rotation, positive XYZ scale, and pivot, `paintBodyFace` for a face material, `sweepBodyFace` for a baked Body-local line-profile sweep along an open or closed planar path, `arrayBodyLinear` or `arrayBodyCircular` for whole-Body clones, and `unionBodies`, `subtractBodies`, or `intersectBodies` for the supported two-Body planar solid booleans.',
  '- Use `offsetBodyFace` only for a closed planar line-edged Body face with no source inner loops. The signed distance in metres is canonical: `distance > 0` performs a nested outward offset and `distance < 0` performs an inward offset. Apply the deterministic miter rule at every corner. Inward offset preserves the source ring and creates one inset face; nested outward offset requires exactly one reciprocal coplanar enclosing host face, preserves the host outer region, and creates one annular face. Preserve the source-region materials and UV frames plus all stable existing ids, and return a deterministic topology remap for created and split features. Reject zero or non-finite distance, a missing face, invalid Body topology, open topology or a non-closed solid, an invalid outer loop, and non-planarity before offset construction. Also reject source inner loops, referenced non-line curves, missing or ambiguous hosts, exterior outward offsets, contact with or crossing of the host boundary or any sibling hole, parallel or collinear degenerate joins, collapse, reversed winding, and self-intersection. Do not trim, repair, or choose another host.',
  '- The canonical Body operation ids are `pushPullBodyFace`, `imprintBodyFace`, `splitBodyFace`, `offsetBodyFace`, `transformBody`, `paintBodyFace`, `sweepBodyFace`, `arrayBodyLinear`, `arrayBodyCircular`, `unionBodies`, `subtractBodies`, `intersectBodies`, `outerShellBodies`, `trimBodies`, and `splitBodies`; read `pascal://modeling/operations` for their typed input fields, canonical metres/radians, preview, commit, and supported surfaces. Return `op splitBodyFace` for an open 2..256-point seam, `op offsetBodyFace`, `op sweepBodyFace`, an exact array operation, or an exact Body Boolean/Solid Tools operation with the manifest fields and do not invent a second operation enum.',
  '- Use `sweepBodyFace` only for one open, hole-free, convex, line-edged planar profile and finite Body-local path points. The first path point lies on the profile plane and the first segment is parallel to the profile normal; later stations form a planar piecewise-linear path. Repeat the first station to close it. Curves, holes, concave profiles, non-planar paths, zero-length or reversing segments, self-intersections, and invalid miters are rejected atomically. `pathPoints` are metres; the result is baked, deterministic, and carries a topology remap. The direct editor exposes Follow Path only in 3D; internal AI and MCP use the same canonical operation.',
  '- Direct Body Arc authoring is available in both 3D and the floor plan with the same three-click start/through/end gesture. It persists three vertices, two `circular-arc` curves attached through `halfEdge.curveId`, and one closing chord; both surfaces sample those curve intervals through the shared deterministic loop-boundary sampler. The authored circle remains mathematical and deterministic across save/load; the 2..256 segment setting controls preview and display sampling only. Initial planar-face Push/Pull fails closed for curved boundaries because the current half-edge extrusion cannot create reciprocal curved side surfaces, and it leaves scene and history unchanged.',
  '- Use `makeMaterialSeamless` for cached seamless texture processing.',
  '- Use `createRoundedRectangularFrameBody` for the supported rounded hollow frame and `createFurniture` plus the furniture bay/tier operations for deterministic cabinet work.',
  '- img3d is a direct editor item-authoring workflow, not a scene-patch operation. It sends one validated reference image and explicit metre dimensions to the authenticated AI provider, accepts only the bounded sculpt JSON schema, compiles that data through Pascal-owned trusted Three.js primitives into an embedded GLB, stores it as a browser-local `asset://` asset, and then uses the ordinary item placement tool. Never return or execute generated TypeScript, JavaScript, arbitrary glTF JSON, URLs, textures, or scripts for img3d.',
  '- Validate the whole plan before application. If one patch is invalid, do not partially apply the plan.',
  '',
  '## MCP Agent',
  '',
  '1. Read `pascal://agent-guide` at the beginning of the task, then read `pascal://scene/current/summary` or query only the target nodes.',
  '2. Call `get_session_context` before mutating. For a new project, call `create_project`; for an existing project, call `open_project` with its id. One MCP server session has one explicit active project binding.',
  '3. Use `create_house_from_brief` for fast generation; it creates a project when none is active and otherwise replaces the explicitly active project. `create_from_template` follows the same active-project rule when saving. For precise work use semantic construction, room, opening, furnishing, placement, and query tools.',
  '4. Use `apply_patch` only for exact bulk edits or capabilities without a semantic MCP tool. Do not rely on internal AI-to-MCP automatic dispatch.',
  '5. Save browser-visible progress with `save_scene` and `saveMode: "draft"`. Use `saveMode: "checkpoint"` only for a meaningful milestone.',
  '6. `get_project_status` is read-only and never activates a project. Use `open_project` when a different project must become active.',
  '7. Finish with `validate_scene`, `verify_scene`, `get_project_status`, and `get_session_context`. Return the exact `editorUrl` from tool output.',
  '8. When live-sync reports a version conflict, reload the scene before retrying. Never overwrite a newer graph blindly.',
  '9. Before any Body semantic edit, read `pascal://modeling/operations`; call `preflight_modeling_operation` with a canonical operation id, the exact Body `nodeId`, and its typed `input` (`faceId` plus signed distance/profile/path, transform fields, material, or `toolBodyId` for `unionBodies`, `subtractBodies`, or `intersectBodies`). Preflight dispatches the manifest operations exhaustively and changes neither scene nor history.',
  '10. Commit a validated Body operation with `commit_modeling_operation` using the same operation id, node id, and typed input boundary. A successful commit applies exactly one logical update, creates one undo step, publishes live sync, and for paint stores the SceneMaterial together with its `scene:` face ref; a `unionBodies`, `subtractBodies`, or `intersectBodies` commit updates the target and deletes the tool in that same undo step. An invalid request returns structured diagnostics without mutation.',
  '11. Every topology-changing Body result includes `topologyRemap`. MeasurementNode and ConstructionDimension anchors owned by the editor are remapped atomically: preserved features keep their id, merged features use the destination, split features keep the original id when it survives or choose the first surviving destination, and deleted features fall back to their stored point. Host-owned annotations are external; reconcile them from the returned remap instead of expecting the editor to mutate them.',
  '',
  '## Direct Editor Interactions',
  '',
  '- Selecting or re-clicking any scene element is inert: selection alone never starts movement or another modeling gesture. Move an element only after explicitly choosing Move from its action menu or pressing the Move shortcut. A selected Body likewise requires the explicit 3D action menu to arm Rotate, Scale, Push/Pull, Offset, or Follow Path; later face and point clicks belong only to that armed action.',
  '- In 3D, Body Move follows a two-click point-to-point workflow: activate Move, click a vertex, edge point, edge midpoint, face point, face center, or Body center as the base, then click the same inference types on another element to place that exact base point there. These object inferences are active in the default grid mode and the lines mode; Off disables them. Snapped feedback names the inference in Korean (꼭짓점, 선 중점, 선 위, 면 중심, 면 위, 요소 중심). Hold Alt while choosing the destination to bypass inference snapping for deliberate free movement or overlap. In the floor plan, the Move action intentionally uses the 2D-owned placement overlay and plan snapping instead of mounting the 3D base-point tool.',
  '- Body Move evaluates only inference candidates belonging to the visible Body face under the pointer and inside a 16 CSS-pixel screen aperture, then applies strict priority: endpoint > midpoint/center (edge midpoint, face center, or Body center) > edge > face. Hidden or occluded topology cannot steal the pointer, a closer lower-tier candidate never beats an eligible higher tier, and a higher-tier candidate outside the aperture never steals the pointer. Base and destination use the same rule; feedback uses one constant 8px screen-space square for every type, colored green for endpoints, cyan for midpoint/center, red for edges, and blue for faces, while Korean labels and the persistent base/live target remain visible.',
  '- With a Body selected in 3D select mode, clicking inside the 16 CSS-pixel inference aperture selects the persistent vertex or half-edge id instead of a render triangle; clicking elsewhere selects the persistent face id. Edge midpoints select their owning edge. The selected vertex or edge is shown with the cyan editor overlay, and topology-changing operations must discard a stale feature id or remap it to the surviving feature.',
  '- In 3D, select a persistent Body vertex, half-edge, or face and then choose Move to reuse the same two-click inference, snapping, Alt-free, and typed-length workflow for that feature. The preview stays ephemeral and moves the selected vertex, both endpoints of the selected half-edge, or every boundary vertex of the selected face. Commit creates one undo step, preserves topology ids, material refs, and UV frames, and increments the Body revision; Escape restores the original Body. A move that would collapse an edge or make any incident face degenerate or non-planar is rejected atomically and marks the length preview invalid. Curved-edge feature movement remains unsupported. The floor plan continues to move the whole Body because sub-entity manipulation is a 3D operation.',
  '- Body Move Autofold is an opt-in 3D preference, off by default. When enabled for a selected vertex, half-edge, or face, an affected non-planar hole-free line-edged face is deterministically fan-triangulated from its first boundary vertex. Existing boundary ids, face surface/material/UV data, and reciprocal topology are preserved; collapse, degenerate triangles, holes, curves, and invalid topology are rejected atomically. The floor plan does not expose Autofold and whole-Body moves remain unchanged.',
  '- Body arrays target the selected whole Body only and leave the original in place. `arrayBodyLinear` takes an integer `count` from 2 to 100 and a finite metre `offset`; each clone is at `i * offset` for `i = 1..count-1`. `arrayBodyCircular` takes `count`, finite metre `center`, a non-zero `axis`, and a signed radian `angle`; `fullCircle: true` defaults to 2π and spaces clones at `i * angle / count`, while a partial array uses `i * angle / (count - 1)`. Clone node ids are fresh, internal Body topology ids remain stable within each clone, child/material references remain valid, and all clones commit in one undo step. Invalid input or cancel leaves scene and history unchanged. The 3D action menu exposes both forms; floor plan exposes linear and Y-axis circular arrays.',
  '- `unionBodies`, `subtractBodies`, and `intersectBodies` accept exactly two valid closed manifold, hole-free, planar, line-edged Bodies in the same parent coordinate frame; concave planar solids are supported. The target is the primary `nodeId`, the second Body is `toolBodyId`, and the operation is evaluated as target union tool, target minus tool, or target intersection tool. Union and subtraction require a positive-volume result; intersection also requires a positive-volume overlap. Non-overlap, touching-only, holes, curves, non-planar/degenerate faces, unsupported multi-shell results, and differing parent frames are rejected atomically. Preview is read-only. Commit returns the target result, deletes the tool, and remains one undo step; undo restores both Bodies. Result faces retain source surface/material/UV provenance where available, while subtraction cut faces use deterministic tool provenance.',
  '- Solid Tools reuse the same two-Body CSG boundary and are available in both 3D and floor plan menus. `outerShellBodies` returns the exterior union and consumes the tool. `trimBodies` returns target minus tool while preserving the tool unchanged. `splitBodies` atomically returns non-empty pieces in fixed target-only, intersection, tool-only order; it omits only CSG-empty pieces and rejects a result with fewer than two pieces. All three use one undo step, and split creates at most one additional Body.',
  '- `inspectBodySolid` (available from the Solid Inspector action) reports topology validity, closed/manifold/orientation/connectivity, signed and absolute volume, degenerate/non-planar faces, and stable feature-id diagnostics without mutating the scene.',
  '- Body Rotate uses the shared pivot state machine in both 3D and the floor plan: place the pivot, place the reference arm, then set the angle. It updates the Body topology as a preview and commits one undo step while preserving persistent topology ids, materials, and UV frames. When a persistent vertex, half-edge, or face is selected in 3D, Rotate snapshots that feature at gesture start and transforms only its stable vertex set; stale, collapsed, curved, or non-planar results are rejected atomically, with deterministic Autofold remapping when explicitly enabled. Escape or cancel restores the original selection and clears the action.',
  '- Body Scale uses the existing six linear-resize handles in 3D: activate Scale, drag one X/Y/Z side handle, and keep the opposite face fixed while grid snapping and the existing HUD report the live dimension. Scale is one-axis-at-a-time, preserves topology ids/materials/UV frames, keeps the preview ephemeral, commits one undo step, and Escape or cancel restores the original selection and clears the action. A selected persistent vertex, half-edge, or face is snapshotted at drag start and receives the same canonical affine scale while unselected Body vertices remain unchanged. Body Rotate uses the shared pivot state machine around X, Y, or Z in 3D; the floor plan remains whole-Body and Y-only.',
  "- Body Push/Pull is an explicit 3D face operation and is intentionally omitted from the floor-plan action menu: activate it from the 3D Body action menu, click a valid face, and the existing drag begins immediately after that face click. It accepts only the selected Body face, keeps distance previews ephemeral, commits one undo step, and clears the armed action on commit or cancel. Cursor distance is the dot product of cursor minus the face anchor with the unit face normal, so the cursor spatial relation to the face normal determines the sign rather than screen left/right. In Lines mode, semantic object snapping resolves other elements' faces, edges, and points while excluding the active Body; Grid quantizes the scalar distance and Off follows the raw cursor. Shift (tap) cycles the existing structural polygon snapping HUD through Grid, Lines, and Off; it never bypasses snapping. Typed distances use the shared unit-aware length input: preserve the live cursor sign and apply the typed positive magnitude. If a rejected preview is invalid, clear the live override and current distance, remain in the armed action, and require a later valid preview before commit. Curved planar boundaries are rejected before extrusion with no scene or history mutation because the current half-edge representation cannot yet produce reciprocal curved side surfaces.",
  '- Body Offset is an explicit 3D face operation and is intentionally omitted from the floor-plan action menu. Its exact action sequence is: select the Body, click Offset in the existing floating 3D action menu, click an eligible planar line-edged face, move perpendicular to the nearest clicked profile edge in the face plane, then click or press Enter to commit. The canonical sign is `distance > 0` outward and `distance < 0` inward; exterior is positive and interior is negative regardless of loop winding. The shared unit-aware input accepts a signed metre or imperial value, with an explicitly typed sign authoritative over the live cursor sign. Execute preview and commit through the same core operation from the same original Body and input: the ephemeral preview equals the committed result. A successful gesture commits one topology update and one undo step. A rejected preview clears only the live override and remains armed; Escape or cancel, tool switch, selection loss, and unmount clear the preview and action while leaving scene and history unchanged.',
  '- Body Follow Path is an explicit 3D face operation and is intentionally omitted from the floor-plan action menu: activate it from the 3D Body action menu, click an eligible planar convex line-edged face, click successive Body-local path stations, and commit an open path with Enter or click and a closed path by repeating the first station or pressing C. The first station lies on the profile plane and the first segment follows the profile normal. Use the shared snapping scope, Alt raw bypass, typed length input, live preview, Escape cancellation, one topology update, one undo step, and atomic invalid-preview rejection. The resulting baked sweep renders in the floor plan, but the floor plan has no sweep edit affordance.',
  '- Face imprint accepts only a planar line-edged closed face with no existing inner loops; the profile must be coplanar, strictly inside, non-self-intersecting, and must not touch the boundary. It preserves existing half-edge ids/materials/UV frames and gives the inset/reveal faces deterministic ids. A signed negative distance reaching or passing the nearest blocking plane is a through-cut; the cut prism is rebuilt from the original source face and committed as the same `imprintBodyFace` operation with a target-only deterministic topology remap. The 2D surface only renders the resulting Body and does not sketch or extrude the face.',
  '- `splitBodyFace` accepts a planar Body face with no inner loops and an open `pathPoints` polyline of 2..256 coplanar points. The first and last points must be distinct host outer-boundary vertices or interiors of straight boundary edges; interior points and segments must remain strictly inside the connected coplanar face arrangement. A path may cross existing coplanar seams: intersected faces are split into deterministic cells, shared intersection vertices and bilateral reciprocal seams are preserved, and the operation is atomic. Reject curves, holes, degenerate or non-planar paths, boundary-following/touching, self-crossing, tangent or collinear seam overlap, and branching seam intersections. Boundary-interior endpoints split both the host half-edge and its reciprocal side-face half-edge when present; seams preserve original face/loop/edge ids on deterministic sides, copy material/UV frames, validate topology and solid volume, and return a topology remap. Direct Line-on-face Enter uses `splitBodyFace`; an Arc uses its sampled open path on Enter and its sampled path plus closing chord on C to call `imprintBodyFace`. This is 3D-only; 2D renders the resulting faces without a subentity split affordance.',
  '- In Items > img3d, choose one PNG/JPEG/WebP reference image up to 5 MB, enter the intended width, height, and depth in metres, generate the bounded model, then click the normal floor placement location. The entered dimensions are authoritative: Pascal normalizes the trusted compiled geometry to that exact size, and the placement click commits the ordinary Item as one undo step. Generated `asset://` GLBs persist and reload in the same browser profile and are included by the browser GLB export path.',
  '- Programmatic agents do not simulate pointer gestures. Express the same final state with deterministic operations and explicit coordinates.',
  '- `groupBodies` and `createComponent` accept only selected sibling Bodies and persist a one-level transform container. Outside an editor-only isolated Body-container edit context, selecting a descendant Body resolves to its `body-group` or `component`; entering edit is transient and selects the first child, while Escape or Exit returns to the container without a history entry. Inside the active container, direct Body children remain selectable and nodes outside it are rejected. `makeComponentUnique` and `explodeComponent` target stable component ids; AI and MCP clients target stable node ids directly, and no editor edit-context flag is persisted in scene data.',
  '',
  '## Design Ontology',
  '',
  '1. Read `pascal://ontology/manifest` before relying on ontology-backed classification; the manifest pins Pack id `pascal-architecture-core`, semantic version `1.0.0`, MIT license, and artifact hashes.',
  '2. Use `query_design_ontology` for bounded class, allowed-operation, relationship, and evidence lookup. Use `explain_design_rule` for an evidence-backed rule such as `pascal:rule/window-hosted-by-wall`.',
  '3. Treat BOT, IFC, and bSDD identifiers as mappings only. Pascal ids and the Pack/class/version semantic ref are the stable execution boundary.',
  '4. For a Window, resolve the actual host Wall from the scene before applying an opening operation. If the ontology pack is unavailable or invalid, keep existing modeling operations available and do not mutate scene JSON or history as part of ontology lookup.',
  '5. Internal AI requests carry semantic refs only for selected nodes. Do not copy the ontology graph into an AI prompt; query the MCP surface when evidence or relationships are needed.',
  '',
  '## Completion Check',
  '',
  '- The requested geometry exists at the intended dimensions and hierarchy.',
  '- `validate_scene` succeeds and `verify_scene.hasIssues` is false, or remaining limitations are stated precisely.',
  '- The mutation is visible in the editor and undo restores the prior state when the operation is undoable.',
  "- External agents return the final `editorUrl`; the internal agent returns a concise message in the user's language plus the smallest valid patch set.",
  '',
  '## Keeping This Manual Current',
  '',
  'Any change that adds or changes a modeling operation, MCP tool/resource/prompt, node capability, interaction modifier, unit rule, validation rule, or AI control path must update this manual in the same change. Remove obsolete instructions instead of accumulating compatibility advice. Tests must prove that both the MCP resource and the internal modeling prompt still expose the current manual.',
].join('\n')
